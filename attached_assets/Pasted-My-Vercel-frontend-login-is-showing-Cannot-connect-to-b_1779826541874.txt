My Vercel frontend login is showing:

"Cannot connect to backend. Please try again later."

Also, this backend health URL does not work anymore:

https://6d08a13c-3c1f-48e8-b970-dcc115e8a8cf-00-20cuy1w6bh9l0.sisko.replit.dev/api/auth/health

Please diagnose and fix the Replit backend.

Project context:
- Backend: Spring Boot Maven
- Database: Supabase PostgreSQL
- Frontend: Vercel plain HTML/CSS/JS
- Backend: Replit
- I only need login/signup working now
- Forgot password should stay moderator-handled only
- Do not re-enable email/SMTP
- Do not require mail secrets

Current required endpoints:
GET /api/auth/health
POST /api/auth/signup
POST /api/auth/login
POST /api/auth/forgot-password

Expected behavior:
- Health returns 200
- Fake login returns 401
- Wrong password returns 401
- Correct login returns 200 with token
- Signup success returns 201
- Duplicate signup returns 409
- Forgot password returns 200 with moderator-help message

Please do these steps:

1. Check whether backend is running:
   - Check the Replit workflow/console
   - Run:
     curl -i http://localhost:5000/api/auth/health
   - Also test port 8080 if needed:
     curl -i http://localhost:8080/api/auth/health

2. If backend is not running, start it:
   mvn spring-boot:run

3. If it crashes, find the exact error and fix it.

4. Check required Replit Secrets:
   - DB_URL
   - DB_USERNAME
   - DB_PASSWORD
   - JWT_SECRET

5. Confirm DB_URL is the real Supabase JDBC URL, not the literal text "DB_URL".
   It should look like:
   jdbc:postgresql://...supabase.com:6543/postgres?sslmode=require

6. Keep forgot-password simple:
   - Do not add spring-boot-starter-mail
   - Do not require Gmail SMTP
   - Do not require JavaMailSender
   - /forgot-password should return 200 with moderator-help message
   - /reset-password can stay disabled

7. Confirm CORS is still open temporarily for Vercel testing.
   Do not lock CORS yet.

8. After fixing, give me:
   - The working public Replit backend base URL
   - The working health URL
   - The exact config.js value I should use:
     const APP_CONFIG = {
         API_AUTH_BASE_URL: "WORKING_BACKEND_URL/api/auth"
     };

9. Run final tests:
   - GET /api/auth/health
   - POST /api/auth/login with fake email
   - POST /api/auth/signup with test email
   - POST /api/auth/forgot-password

Do not restructure files.
Do not change frontend files.
Do not re-enable forgot password email reset.